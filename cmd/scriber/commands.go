package main

// Subcommand builders live in:
//   daemon.go  — daemonCmd
//   cli.go     — attachCmd / detachCmd / streamCmd / selectCmd / cycleCmd / pasteCmd / fixCmd / monitorCmd / doctorCmd
//   doctor.go  — runDoctor (used by doctorCmd)
