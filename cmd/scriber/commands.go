package main

// Subcommand builders live in:
//   daemon.go  — daemonCmd
//   cli.go     — attachCmd / detachCmd / listCmd / switchCmd / cycleCmd / statusCmd / doctorCmd
//   doctor.go  — runDoctor (used by doctorCmd)
